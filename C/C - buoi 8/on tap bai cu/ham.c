#include <stdio.h>

int main() {
	/*1. Hàm không trả về giá trị và không nhận tham số đầu:
	kiểu_trả_về phải là kiểu void, nếu không có tham số thì phần trong cặp () trống, hoặc để void, ví dụ: nhapLieu(void)
	  2. Hàm nhận tham số đầu và không trả về giá trị
	  3. Hàm trả về giá trị và không có tham số đầu.
	  4. Hàm trả về giá trị và nhận tham số đầu*/
	return 0;
}